<div class="page-header">
  <h3>Request #<%= @request.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt>Requester</dt>
      <dd><%= @request.requester_id %></dd>

      <dt>Keyword</dt>
      <dd><%= @request.keyword_id %></dd>

      <dt>Action</dt>
      <dd><%= @request.action_id %></dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/requests" class="btn btn-primary">
        Back
      </a>
      <% if current_user == @request.requester %>
        <a href="/requests/<%= @request.id %>/edit" class="btn btn-warning">
          Edit
        </a>
        <a href="/delete_request/<%= @request.id %>" class="btn btn-danger" rel="nofollow">
          Delete
        </a>
      <% end %>
    </div>
  </div>
</div>
