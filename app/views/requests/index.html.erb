<div class="page-header">
  <h1>
    Requests
    <a href="/requests/new" class="btn btn-lg btn-success">New Request</a>
  </h1>
</div>

<div class="row">
  <div class="col-md-12">
    <table class="table table-striped table-hover">
      <tr>
        <th>Requester</th>
        <th>Keyword</th>
        <th>Action</th>
        <th>Actions</th>
      </tr>

      <% @requests.each do |request| %>
      <tr>
        <td><%= request.requester.email %></td>
        <td><%= request.keyword.created_at %></td>
        <td><%= request.action.created_at %></td>
        <td>
          <a href="/requests/<%= request.id %>" class="btn btn-primary">Show</a>
          
          <% if current_user == request.requester %>
            <a href="/requests/<%= request.id %>/edit" class="btn btn-warning">Edit</a>
            <a href="/delete_request/<%= request.id %>" class="btn btn-danger" rel="nofollow">Delete</a>
          <% end %>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>
